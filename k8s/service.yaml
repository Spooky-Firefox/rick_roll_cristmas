apiVersion: v1
kind: Service
metadata:
  name: rickroll
  labels:
    app: rickroll
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: rickroll
